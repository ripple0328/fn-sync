import QtQuick

// All plugin-rendered strings are plain text. Several values originate from
// NAS directory listings, controller output, and user-supplied task names.
Text {
    textFormat: Text.PlainText
}
