#!/bin/sh
# Compatibility command for installations and scripts created before 0.3.0.
set -eu

if command -v fn-sync >/dev/null 2>&1; then
  exec fn-sync "$@"
fi

script_dir=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
exec "$script_dir/fn-sync" "$@"
